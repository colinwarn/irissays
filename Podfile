# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wait But Why Reader' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wait But Why Reader

  target 'Wait But Why ReaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Wait But Why ReaderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
